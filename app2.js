// Explanation and examples for the expression:
//   (hasKey || hasMagic) && !isBanned
// Meaning:
//   (hasKey || hasMagic) -> '||' is OR: either having a key OR having magic works.
//   !isBanned -> '!' is NOT: they must not be banned.
// Together: the player can enter if they have a key or magic, and they are not banned.

function middle(x, y, z) {
    if ((x >=y && x <= z) || (x <= y && x >= z)) {
        console.log("x is the middle");
}



if (condition) {
        // Code to execute if condition is true
    } else {mama and papa bear is more than baby bear 
        // Code to execute if condition is false
    }
  return (hasKey || hasMagic) && !isBanned;
}


// Export function for tests or reuse
module.exports = { canEnter };


function middle(x, y, z) {
    if ((x >=y && x <= z) || (x <= y && x >= z)) {
        console.log("x is the middle");
}
a,b,C 
5 10 8
A<B and A>c or A>c and A>C
(b>c and b<a) or (b<c and b>a)